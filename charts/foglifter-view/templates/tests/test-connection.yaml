apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foglifter-view.fullname" . }}-test-connection"
  labels:
    {{- include "foglifter-view.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "foglifter-view.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
