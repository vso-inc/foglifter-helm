{{- $imageUpdater := .Values.imageUpdater | default dict }}
{{- if $imageUpdater.enabled }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Release.Name }}-image-updater
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    chart: argocd-image-updater
    repoURL: https://argoproj.github.io/argo-helm
    targetRevision: {{ (.Values.imageUpdater).version | semver }}
    helm:
      values: |
        config:
          log.level: debug
          {{- if (.Values.imageUpdater).registries }}
          registries:
            {{- default list .Values.imageUpdater.registries | toYaml | nindent 12 }}
          {{- end }}
        extraObjects:
          - {{- tpl (.Files.Get "files/image-updater-cr.yaml") . | nindent 12 }}
  destination:
    server: "https://kubernetes.default.svc"
    namespace: {{ .Release.Namespace }}
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
      - FailOnSharedResource=false
    automated:
      selfHeal: true
      prune: true
{{- end }}
