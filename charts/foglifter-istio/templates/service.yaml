apiVersion: v1
kind: Service
metadata:
  name: {{ include "foglifter-istio.fullname" . }}
  labels:
    {{- include "foglifter-istio.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "foglifter-istio.selectorLabels" . | nindent 4 }}
