{{- if .Values.compliance.enabled }}
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: {{ .Release.Name }}-compliance-api-http
spec:
  entryPoints:
    - web
  routes:
    - match: {{ include "traefik.hosts" .Values }}{{ include "traefik.path" .Values.compliance }}
      kind: Rule
      middlewares:
        - name: https-redirect
          namespace: {{ .Release.Namespace }}
      services:
        - name: {{ default "foglifter-compliance-api-svc" (.Values.compliance).name }}
          namespace: {{ default "foglifter" .Values.foglifterNamespace }}
          port: {{ default "3000" (.Values.compliance).port | int }}
    {{- if .Values.traefikService }}
    - match: {{ printf "Host(`%s`) && " .Values.traefikService }}{{ include "traefik.path" .Values.compliance }}
      kind: Rule
      services:
        - name: {{ default "foglifter-compliance-api-svc" (.Values.compliance).name }}
          namespace: {{ default "foglifter" .Values.foglifterNamespace }}
          port: {{ default "3000" (.Values.compliance).port | int }}
    {{- end }}
{{- end }}
