apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foglifter.fullname" . }}-test-connection"
  labels:
    {{- include "foglifter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "foglifter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
