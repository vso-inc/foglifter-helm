apiVersion: v1
kind: Service
metadata:
  name: {{ include "foglifter.fullname" . }}
  labels:
    {{- include "foglifter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "foglifter.selectorLabels" . | nindent 4 }}
